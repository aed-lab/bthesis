# ============================================================
# latexmkrc
# upLaTeX -> DVI -> dvipdfmx -> PDF
# ============================================================

# ------------------------------------------------------------
# LaTeX
# ------------------------------------------------------------
$latex = 'uplatex'
       . ' -interaction=nonstopmode'
       . ' -halt-on-error'
       . ' -synctex=1'
       . ' -file-line-error'
       . ' -kanji=utf8'
       . ' %O %S';

# ------------------------------------------------------------
# Bibliography
# ------------------------------------------------------------
$bibtex = 'upbibtex'
        . ' -kanji=utf8'
        . ' %O %B';

# ------------------------------------------------------------
# DVI -> PDF
# ------------------------------------------------------------
$dvipdf = 'dvipdfmx %O -o %D %S';

$pdf_mode = 3;

# ------------------------------------------------------------
# Output directory
# ------------------------------------------------------------
$out_dir = 'out';

# ------------------------------------------------------------
# Viewer
#
# PDF表示は LaTeX Workshop に任せる
# ------------------------------------------------------------
$view = 'none';

# ------------------------------------------------------------
# Search paths
# ------------------------------------------------------------

# .tex / .sty / .cls / images
ensure_path(
    'TEXINPUTS',
    './sty//',
    './src//',
    './figs//'
);

# BibTeX style (.bst)
ensure_path(
    'BSTINPUTS',
    './sty//'
);

# BibTeX databases (.bib)
ensure_path(
    'BIBINPUTS',
    './bib//'
);

